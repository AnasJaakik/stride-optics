{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip uninstall -y opencv-contrib-python opencv-python 2>/dev/null; pip install opencv-python-headless --force-reinstall --no-deps 2>/dev/null || true; cd backend && python app.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

